# Supabase CLI local state
supabase/.branches/
supabase/.temp/
supabase/.env

# Environment files — never commit keys. The service_role key in particular is
# a full compromise of every project's data if it leaks (DB R-RLS-3).
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Node, if console tooling lands in this repo
node_modules/

# Editor and OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/
